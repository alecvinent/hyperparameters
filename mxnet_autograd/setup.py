from setuptools  import setup

setup(name='hypergrad_test',
      #version=1,
      description='hypergrad',
      packages=['hypergrad_test'],
     )
      #data_files=[('mxnet', [LIB_PATH[0]])],
      #url='https://github.com/dmlc/mxnet',
      #ext_modules=config_cython())
